{
  "name": "sketchy",
  "version": "1.0.0",
  "description": "",
  "main": "",
  "scripts": {
    "build": "tsc",
    "dev": "concurrently --kill-others 'tsc -w' 'http-server . -c-1 -s'",
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "Alexander Ivanov",
  "license": "MIT",
  "devDependencies": {
    "concurrently": "^8.0.1",
    "http-server": "^14.1.1",
    "typescript": "^5.0.4"
  }
}
